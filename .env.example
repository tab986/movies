# Server
PORT=5000
NODE_ENV=production

# Postgres — user postgres, URL-encode special chars in password (@ → %40, ! → %21)
DATABASE_URL=

# Auth — long random string for JWT signing
JWT_SECRET=

# TMDB — https://www.themoviedb.org/settings/api
TMDB_READ_ACCESS_TOKEN=
TMDB_API_KEY=
